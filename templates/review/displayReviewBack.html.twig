{% extends 'admin/NavDash.html.twig' %}
{% block body %}

    <div class="row" style="display: flex;align-items: center;justify-content: center;margin-top: 20px;">

        <div class="col-lg-8">

            <div class="card">
                <div class="card-body">
                    <h3>Liste des commentaires</h3>
                    <!-- Table with stripped rows -->
                    <table class="table table-striped">
                        <thead>
                        <tr>
                            <th>Commentaire</th>
                            <th>Supprimer</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for r in reviews %}
                            <tr>
                                <td>{{ r.commentaire }}</td>

                                <td>
                                    <a class="btn btn-primary" href="{{ path('supp_reviewBack',{'id':r.id}) }}">Supprimer</a>
                                </td>
                            </tr>

                        {% endfor %}
                        </tbody>

                    </table>
                </div>
            </div>

        </div>



    </div>
{% endblock %}

