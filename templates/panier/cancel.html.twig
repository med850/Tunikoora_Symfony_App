{% extends 'home/index.html.twig' %}

{% block body %}
<section class="bg-light p-3">
    <h1>Votre Commande</h1>

</section>
<div class="row" style="display: flex;align-items: center;justify-content: center;margin-top: 150px;">
    <div class="col-lg-6">

        <div class="card-body">

            <table border="1">
                <table class="table">
                    <td>
                        <h1>Votre commande n'est pas valide veuillez reessayer</h1>
                        <a class="btn btn-danger" href="{{ path('cart') }}">echec</a></td>

                    <tfoot>
                    <tr>
                    </tr>
                    </tfoot>
                </table>


                {% endblock %}