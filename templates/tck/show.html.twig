{% extends 'home/index.html.twig' %}
{% block title %}Ticket{% endblock %}

{% block body %}
    <h1>Ticket</h1>
<div class="row" style="display: flex;align-items: center;justify-content: center;margin-top: 150px;">
    <div class="col-lg-6">
        <div class="card">
            <div class="card-body">

                <table border="1">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ticket.id }}</td>
            </tr>
            <tr>
                <th>Equipea</th>
                <td>{{ ticket.equipea }}</td>
            </tr>
            <tr>
                <th>Equipeb</th>
                <td>{{ ticket.equipeb }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ ticket.prix }} DT</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-success" href="{{ path('app_tck_index') }}">back to list</a>

    <a href="" class="btn btn-success">Panier</a>
{% endblock %}
